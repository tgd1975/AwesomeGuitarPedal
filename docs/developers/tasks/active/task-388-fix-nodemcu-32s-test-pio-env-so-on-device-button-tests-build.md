---
id: TASK-388
title: Fix nodemcu-32s-test PlatformIO env so on-device button tests build
status: active
opened: 2026-05-11
effort: Small (<2h)
complexity: Junior
human-in-loop: No
---

## Description

The `nodemcu-32s-test` PlatformIO environment (and its on-device test
siblings) cannot build today. `make test-esp32-button` aborts in the
compile stage on a clean `main` checkout with two distinct errors:

1. **Missing header on the include path.**

   ```text
   src/esp32/include/button.h:2:10: fatal error: i_button.h: No such file or directory
    #include "i_button.h"
             ^~~~~~~~~~~~
   compilation terminated.
   *** [.pio/build/nodemcu-32s-test/test/test_buttons_esp32/test_main.cpp.o] Error 1
   ```

   `i_button.h` lives at [`lib/PedalLogic/include/i_button.h`](../../../../lib/PedalLogic/include/i_button.h).
   The production env (`nodemcu-32s`) resolves it fine through PlatformIO's
   Library Dependency Finder; the test env does not. The two envs differ
   in `build_src_filter` and `build_flags` but not in `lib_deps`/`lib_ldf_mode`,
   so the regression is in how the test env walks `lib/PedalLogic/`.

2. **`FakeSerial` shadows the real `HardwareSerial` and breaks Unity's
   serial output config.**

   ```text
   unity_config.cpp:39:47: error: 'struct FakeSerial' has no member named 'write'
   unity_config.cpp:40:38: error: 'struct FakeSerial' has no member named 'flush'
   unity_config.cpp:41:41: error: 'struct FakeSerial' has no member named 'end'
   ```

   The test env's `build_flags` includes `-Itest/fakes`, which puts the
   host-test [`arduino_shim.h`](../../../../test/fakes/arduino_shim.h)
   on the include path. `FakeSerial` declares no `write`/`flush`/`end`
   methods, so PlatformIO's autogenerated `unity_config.cpp` fails to
   compile inside the on-device test build. The fakes belong to the
   host (`make test-host`) build only.

Both failures predate the `feature/firmware` branch and reproduce on
`main` (verified by checking out the relevant files from `main` and
re-running the test build). EPIC-028 / TASK-376 closed with on-device
verification done by flashing production firmware rather than running
the Unity test suite — the new debounce scenarios are written and
ready to run in [`test/test_buttons_esp32/test_main.cpp`](../../../../test/test_buttons_esp32/test_main.cpp)
once this task lands.

The same `-Itest/fakes` pattern appears in several sibling on-device
test envs (`nodemcu-32s-multipress-test`, `-serial-test`,
`-profilemanager-test`, `-ble-config-test`, `-leds-test`,
`-gpio2-probe`, `-ble-pairing-test`, `-pin-io-test` — confirm the
exhaustive list in [`platformio.ini`](../../../../platformio.ini)). The
fix should sweep all of them, not just the button env, to avoid the
next on-device test running into the same wall.

## Acceptance Criteria

- [ ] `make test-esp32-button` builds successfully on a clean checkout
      (no `i_button.h` error, no `FakeSerial` errors).
- [ ] Every other ESP32 on-device test env in `platformio.ini` builds
      to completion: `-multipress-test`, `-serial-test`,
      `-profilemanager-test`, `-ble-config-test`, `-leds-test`,
      `-gpio2-probe`, `-ble-pairing-test`, `-pin-io-test` (whichever
      currently exist).
- [ ] With an ESP32 connected via USB, `make test-esp32-button` runs
      the existing Unity scenarios end-to-end, plus the two EPIC-028
      debounce scenarios already added in
      `test/test_buttons_esp32/test_main.cpp`
      (`test_configurable_debounce_250ms_suppresses_fast_double_press`,
      `test_configurable_debounce_250ms_accepts_slow_double_press`).
- [ ] The host (`make test-host`) suite still passes — the fix must
      not regress the host build that legitimately needs
      `arduino_shim.h`.

## Test Plan

**On-device tests** (`make test-esp32-button`):

- Verify the build completes for `nodemcu-32s-test`.
- Run the full Unity test list against a connected ESP32; all existing
  scenarios pass, both new EPIC-028 debounce scenarios pass.

**Sweep build smoke-test for sibling envs** (no device required for
the build step):

- Run `pio run -e <env> --target build` for each on-device test env
  named in the AC. Each must reach `SUCCESS`. Upload + run is not
  required for this task — the build green is the gate.

**Host tests** (`make test-host`):

- Re-run after the fix. The host build still uses `test/fakes/`; if
  this task touches `arduino_shim.h` (e.g. to add the missing Serial
  methods to `FakeSerial`), confirm the existing 312 host tests still
  pass.

## Notes

- **Suspected fix.** The minimal change is to drop `-Itest/fakes` from
  the on-device test envs' `build_flags`: those envs compile real
  Arduino code against a real chip, so the host shim is not just
  unnecessary, it's actively harmful. The header is needed only for
  the CMake-based host build. For the `i_button.h` lookup, verify
  whether the existing `lib_ldf_mode = deep` is enough once the shim
  is out of the way; if not, an explicit `-Ilib/PedalLogic/include`
  may be needed.
- **Out of scope.** Any nRF52840 test env (EPIC-025: hardware
  unreachable). The host `/test` suite. Production firmware envs
  (those already build cleanly). Adding new Unity scenarios — this
  task only restores the build; the scenarios that exist (including
  the two new EPIC-028 ones) should just start working.
- **Why this matters.** Every on-device test env currently shipping
  is unusable today. The button env is the most-affected because
  EPIC-028 added two scenarios to it that depend on a working test
  build, but the same breakage blocks any future on-device test work
  on ESP32. Fixing this once unblocks the whole shape of ESP32 test
  development.
- **Diagnostic shortcut.** The failure is reproducible without
  hardware — just run the build step. Use
  `pio test -e nodemcu-32s-test -f test_buttons_esp32 --without-uploading --without-testing`
  to iterate on the fix without an ESP32 connected.

## Resolution

Fix applied:

1. **Dropped `-Itest/fakes`** from the five ESP32 on-device test envs
   (`nodemcu-32s-test`, `-multipress-test`, `-serial-test`,
   `-profilemanager-test`, `-pin-io-test`). This eliminates the
   `FakeSerial` shadowing of `HardwareSerial` in `unity_config.cpp`.
2. **Split `test/fakes/` into `test/fakes/` (host-only) and `test/stubs/`
   (portable).** Moved `null_led_controller.h` and `null_logger.h` to
   `test/stubs/`. These are pure interface implementations safe to compile
   on-device. Added `-Itest/stubs` to on-device test envs and updated
   `test/CMakeLists.txt` to keep the host build seeing both directories.
3. **Added `-Ilib/PedalLogic/include`** to ESP32 on-device test envs so
   `i_button.h` resolves without relying on LDF for transitively-included
   headers inside `src/`.
4. **Added `test_build_src = yes` and excluded `src/esp32/main.cpp`** from
   `build_src_filter` for the four `pio test`-based envs. Without
   `test_build_src`, `pio test` doesn't compile project sources; without
   excluding `main.cpp`, the framework `setup()`/`loop()` collide with
   Unity's `test_main.cpp` definitions.
5. **Tightened `nodemcu-32s-pin-io-test` build_src_filter** to `-<*>`
   because its lib_deps intentionally exclude BLE; pulling in
   `src/esp32/src/ble_keyboard_adapter.cpp` would fail to find
   `BleKeyboard.h`. The test only needs `pin_action.cpp` from
   `lib/PedalLogic/` which LDF resolves.

Verification (on this branch, ESP32 connected on `/dev/ttyUSB0`):

| Env | Build | Notes |
|---|---|---|
| `nodemcu-32s` (production) | PASS | Unchanged behaviour |
| `nodemcu-32s-test` | PASS | Flashed + ran 13 scenarios end-to-end |
| `nodemcu-32s-multipress-test` | PASS | Build only |
| `nodemcu-32s-serial-test` | PASS | Build only |
| `nodemcu-32s-profilemanager-test` | PASS | Build only |
| `nodemcu-32s-pin-io-test` | PASS | Build only |
| `nodemcu-32s-leds-test` | PASS | Build only |
| `nodemcu-32s-gpio2-probe` | PASS | Build only |
| `nodemcu-32s-ble-config-test` | PASS | Build only |
| Host (`make test-host`) | PASS | 312/312 |

`make test-esp32-button` ran 13 Unity scenarios on device. The 3
non-interactive scenarios (no-press) passed; the 10 interactive
scenarios (e.g. "Press button A ONCE") failed because no human was
present to drive the prompts — these are not build/infra failures.
The two EPIC-028 debounce scenarios
(`test_configurable_debounce_250ms_*`) were compiled, flashed, and
prompted on device — they just need a human button-presser to pass.
